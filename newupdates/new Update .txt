## Tasks for Claude (EN)

**Do nots:**

* Do **not** rewrite files. Touch only what’s listed below.
* Do not change menus, language toggle, or unrelated layout.

**Video tasks:**

1. **HTML attributes**

* Ensure `<video>` has: `autoplay`, `muted`, `playsinline`, `preload="metadata"`.
* Set a poster image (no black flash).
* Use **my new file** as the source: `new-intoMP4.mp4` (update the path to where **I** placed it).
* Remove all old source/preload leftovers.

2. **Responsive layout**

* Video must scale proportionally on desktop & mobile.
* Use `object-fit: cover` for hero/full-bleed; use `contain` if it’s inside a card.
* Keep width at 100% with a fixed `aspect-ratio` (e.g., 16/9).
* No overflow/scrollbars.

3. **Cross-device compatibility**

* Keep `muted+playsinline` for iOS autoplay.
* If autoplay is blocked, add a user-gesture fallback to call `play()` (aligned with current JS flow).

4. **Accessibility**

* Add a meaningful `aria-label` to `<video>`.
* If captions remain, `<track>` must be VTT; otherwise remove `<track>`.
* Keyboard: `Space/Enter` toggles play/pause via existing controls.

5. **Performance**

* Keep `preload="metadata"` to avoid heavy first paint.
* Don’t re-encode; I handled the file.
* No console warnings/errors.

6. **Behavior**

* After intro end or Skip, continue to the next screen seamlessly (respect current app flow).
* Never auto-unmute; volume changes only on user action.
* No unwanted restarts when tab visibility changes.

**Acceptance criteria:**

* Proper proportional rendering on all devices.
* Autoplay works on iOS (muted+playsinline).
* Intro→Skip flow intact.
* **Zero** console errors/warnings.
* No leftover references to the old video.

---

